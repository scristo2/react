#npx |yarn en mi mac expo install expo-constants
import { View, Text, StyleSheet, SafeAreaView } from "react-native";
import { StatusBar } from "expo-status-bar";
import Constants from "expo-constants";

### si te da error con el,ejecutar estos comandos 
yarn add react-native-reanimated react-native-gesture-handler react-native-screens react-native-safe-area-context @react-native-community/masked-view

export default function App() {
  return (
    <SafeAreaView style={styles.container}>
      <View>
        <Text>hello world jjjhshsh</Text>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "orange",
    marginTop: Constants.statusBarHeight, safeareview es solo para ios,para android le ponemos este valor y da lo mismo
  },
});
